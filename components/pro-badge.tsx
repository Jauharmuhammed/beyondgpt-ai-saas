import React from "react";
import { Badge } from "./ui/badge";

const ProBadge = () => {
    return <Badge className="uppercase bg-violet text-white text-sm font-bold">Pro</Badge>;
};

export default ProBadge;
